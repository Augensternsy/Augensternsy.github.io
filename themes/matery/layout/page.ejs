<%- partial('_partial/bg-cover') %>

<main class="content">

    <div class="container">
        <div class="card">
            <div class="card-content">
                <%- page.content %>
                <% if (page.type === 'about' && theme.myProjects && theme.myProjects.enable) { %>
                <%- partial('_widget/my-projects') %>
                <% } %>
            </div>
        </div>
    </div>
</main>
